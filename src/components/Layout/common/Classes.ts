import {getNamespace} from "../../../Utils/Classes";

const NS = getNamespace()

export const LAYOUT = `${NS}-layout`
export const LAYOUT_PANE = `${NS}-layout-pane`
export const LAYOUT_PANE_PRIMARY = `${NS}-layout-pane-primary`
export const LAYOUT_VERTICAL = `${NS}-vertical`
export const LAYOUT_HORIZONTAL = `${NS}-horizontal`
export const LAYOUT_HANDLER = `${NS}-layout-handler`
export const LAYOUT_CHANGING = `${NS}-layout-changing`
