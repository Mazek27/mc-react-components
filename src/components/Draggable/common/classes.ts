const NS = `mc`

export const RESIZE_HANDLE_TARGET = `${NS}-resize-handle-target`;
export const RESIZE_HANDLE = `${NS}-resize-handle`;
export const DRAGGING = `${NS}-dragging`;
export const RESIZE_HORIZONTAL = `${NS}-resize-horizontal`;
export const RESIZE_VERTICAL = `${NS}-resize-vertical`;
